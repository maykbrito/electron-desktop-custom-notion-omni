{
  "name": "electron-notion-omni",
  "version": "1.0.0",
  "description": "Inject some CSS to custom Notion colors ;)",
  "main": "src/main/app.js",
  "scripts": {
    "start": "electron .",
    "build": "npx electron-packager . --icon assets/icon.icns --out=packages",
    "build-win64": "npx electron-packager . Notion --overwrite --out=packages --platform=win32 --arch=x64 app-version=1.0 --icon=assets/icon.ico"

  },
  "devDependencies": {
    "electron": "^9.4.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron-window-state": "^5.0.3"
  }
}
